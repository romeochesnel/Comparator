# Port d'écoute du serveur (défaut: 3000)
PORT=3000

# Clé API pour protéger l'accès (laisser vide = pas d'auth)
API_KEY=change-me

# Chemin vers la base de données SQLite (défaut: data.db à la racine)
# DB_PATH=/chemin/absolu/vers/data.db
